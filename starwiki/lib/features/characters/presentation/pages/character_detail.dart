import 'package:flutter/material.dart';

class CharacterDetailedPage extends StatelessWidget {
  const CharacterDetailedPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
